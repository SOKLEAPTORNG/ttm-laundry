import {StyleSheet} from 'react-native';
import {Color} from '../../Constant';

export const styles = StyleSheet.create({
  topText: {
    fontSize: 14,
    color: Color.DARK,
    textAlign: 'justify',
    marginTop: 10,
  },
  bottomText: {
    fontSize: 14,
    color: Color.DARK,
    textAlign: 'justify',
    marginVertical: 15,
  },
});
