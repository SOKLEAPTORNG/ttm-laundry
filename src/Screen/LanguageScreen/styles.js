import {StyleSheet} from 'react-native';
import {Color} from '../../Constant';
export const styles = StyleSheet.create({
  languageButton: {
    width: '100%',
    height: 55,
    justifyContent: 'center',
    borderBottomWidth: 1,
    borderBlockColor: Color.DARK,
    marginTop: 5,
  },
  languageLabel: {
    fontSize: 20,
    color: Color.DARK,
  },
});
